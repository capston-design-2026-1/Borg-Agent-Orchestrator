# 2026-03-27 16:32 KST Milestone

## Summary

This milestone captures the transition from the original test forecaster workflow to the isolated advanced XGBoost workflow.

Key outcomes:

- Advanced data and model workspace is isolated under `~/Documents/borg_xgboost_workspace`
- Advanced raw downloader now supports the fixed matched-shard plan for clusters `b` through `g`
- Advanced source code is separated under `src/advanced_xgboost`
- Advanced feature engineering now includes richer operational and temporal features
- Missing-data handling for advanced XGBoost keeps label-valid rows, preserves numeric nulls, and adds explicit missingness flags
- Multi-horizon targets are prepared for `5m`, `15m`, `30m`, `45m`, and `60m`
- Repo-local runtime setup is ready through `.venv`, with `polars`, `xgboost`, and macOS `libomp` verified

## Current Advanced Download Snapshot

Observed under `~/Documents/borg_xgboost_workspace/raw` at milestone time:

- `events`: `46` files
- `usage`: `41` files
- `machines`: `3` files

Representative state:

- `b` has machine shard `000000000000` and event/usage shards `000000000000` through `000000000014`
- `c` has machine shard `000000000000` and at least the early event/usage shards present
- `d` has machine shard `000000000000`
- The advanced download is still in progress and downstream processing has not been run on the completed fixed-shard set yet

## Advanced Pipeline Readiness

Ready scripts:

- `./scripts/setup_advanced_runtime.sh`
- `./scripts/run_advanced_download.sh`
- `./scripts/run_advanced_flatten.sh`
- `./scripts/run_advanced_join.sh`
- `./scripts/run_advanced_feature_build.sh`
- `./scripts/run_advanced_train.sh`
- `./scripts/run_advanced_xgboost_pipeline.sh`

Verified runtime:

- `polars 1.39.3`
- `xgboost 2.1.4`

## Next Execution Sequence

After the raw download completes, run:

```bash
./scripts/run_advanced_flatten.sh
./scripts/run_advanced_join.sh
./scripts/run_advanced_feature_build.sh
./scripts/run_advanced_train.sh
```

Or run the full chain:

```bash
./scripts/run_advanced_xgboost_pipeline.sh
```

## Important Notes

- The original baseline/test pipeline remains intact under `~/Documents/borg_data` and `~/Documents/borg_processed`
- The advanced track should continue to use only `~/Documents/borg_xgboost_workspace`
- The advanced trainer is prepared to train one model per target horizon from the same feature parquet without requiring separate joined datasets
