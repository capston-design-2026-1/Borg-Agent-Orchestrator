# Milestone Checkpoint

Timestamp: 2026-03-19 19:33 KST

## Current Repository State

- Branch: `main`
- Remote sync: `origin/main`
- Working tree: clean at checkpoint time

## Work Completed Since The Previous Milestone

- Added local schema explanation files beside the generated parquet and artifact directories under `~/Documents/borg_processed`.
- Documented meanings for raw processed parquet types, joined datasets, forecaster datasets, canonical common forecaster datasets, and baseline artifact directories.
- Added a durable repository rule requiring schema or artifact explanation files to be created next to new parquet or artifact directories under `~/Documents/borg_processed`.
- Recorded the same parquet-schema-note rule in the handoff file for future Codex sessions.

## Current Operational Conventions

- Split commits aggressively by concern and file class.
- Use KST timestamp-prefixed filenames in `reports/` with format `YYYYMMDDHHMM_*`.
- Treat `milestone` as an instruction to persist current state in tracked memory files before ending the session.
- When a new parquet type or artifact directory is created under `~/Documents/borg_processed`, place a schema or artifact explanation file in the same directory.

## Current Next Step

- Replace the example local-cloud mapping with a real telemetry mapping and generate the first canonical local-cloud dataset.
- After that, add a stronger forecaster baseline on top of the canonical schema.
