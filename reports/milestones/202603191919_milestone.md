# Milestone Checkpoint

Timestamp: 2026-03-19 19:19 KST

## Current Repository State

- Branch: `main`
- Remote sync: `origin/main`
- Working tree: clean at checkpoint time

## What Has Been Completed

- Downloaded Borg trace shards with Google Cloud CLI workflow support.
- Flattened raw Borg trace data into per-cluster parquet outputs.
- Built joined usage, event, and machine datasets.
- Built forecaster datasets with 15-minute failure labels.
- Trained and evaluated the first Polars-based forecasting baseline.
- Exported baseline inspection artifacts including cluster metrics, feature ranking, and top-risk alerts.
- Added temporal features and evaluated multiple feature profiles.
- Kept `base` as the strongest default profile and recorded `base_plus_roll` as an alert-focused alternate profile.
- Added canonical forecaster schema export for Borg-derived datasets.
- Added a local-cloud adapter that maps parquet or CSV telemetry into the canonical forecaster schema.
- Added report files for evaluation results and schema reference.
- Tightened commit sharding policy and persisted milestone workflow rules.

## Current Operational Conventions

- Split commits aggressively by concern and file class.
- Use KST timestamp-prefixed filenames in `reports/` with format `YYYYMMDDHHMM_*`.
- Treat `milestone` as an instruction to persist current state in tracked memory files before ending the session.

## Current Next Step

- Replace the example local-cloud mapping with a real telemetry mapping and generate the first canonical local-cloud dataset.
- After that, add a stronger forecaster baseline on top of the canonical schema.
